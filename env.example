# 复制此文件为 .env 并填入你的配置

# API 基础URL
OPENAI_BASE_URL=https://api.openai.com/v1

# API 密钥
OPENAI_API_KEY=your-api-key-here

# ======== 模型配置 ========
# 断句模型 - 负责将长句分割成适合字幕显示的短句
# 推荐：快速模型，如 gpt-4o-mini
SPLIT_MODEL=gpt-4o-mini

# 翻译模型 - 负责将字幕翻译成目标语言
# 推荐：高质量模型，如 gpt-4o 或 claude-3-sonnet
TRANSLATION_MODEL=gpt-4o

# 总结模型 - 负责分析字幕内容并生成摘要
# 推荐：平衡模型，如 gpt-4o-mini 或 claude-3-haiku
SUMMARY_MODEL=gpt-4o-mini

# 兼容性：默认模型 (如果上述模型未设置，将使用此模型)
LLM_MODEL=gpt-4o-mini

# 本地模型建议降低并发，避免过度占用内存和频繁超时
THREAD_NUM=2

# LM Studio 专用：空闲 5 分钟后自动卸载模型
LM_STUDIO_TTL=300

# LM Studio 专用：全部任务完成后主动卸载本次用到的模型
LM_STUDIO_UNLOAD_ON_COMPLETE=true
